namespace Jascartec.Domain.Enums;

/// <summary>Un apunte manual dentro de una caja abierta (no viene de una venta/compra ya
/// registrada) — ej. un gasto chico en efectivo, o un ingreso extra de dinero al cajón.</summary>
public enum TipoMovimientoCaja
{
    Ingreso,
    Salida
}
