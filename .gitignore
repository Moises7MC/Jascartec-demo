# .NET
backend/**/bin/
backend/**/obj/

# Configuración local con contraseña real (no subir a GitHub)
backend/src/Jascartec.Api/appsettings.Development.json

# Configuración con token privado (no subir a GitHub)
config.js

# Caché local de Wrangler (Cloudflare Workers) — no aporta nada al repo
worker/.wrangler
worker/node_modules
